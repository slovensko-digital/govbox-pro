<div class="bg-gray-100" data-controller="slideover" data-action="keydown.esc->slideover#hide" tabindex="-1">
  <%= render TW::MobileSidebarComponent.new %>
  <div class="hidden lg:fixed lg:inset-y-0 lg:z-50 lg:flex lg:w-80 lg:flex-col">
    <%= static_sidebar %>
  </div>
  <div class="lg:pl-80">
    <div class="sticky top-0 z-40 flex items-center h-16 gap-4 px-4 bg-white border-t-0 border-r-0 border-b border-l-0 border-gray-200">
     <%= top_navigation %>
    </div>
    <%= main_content %>
  </div>
</div>
