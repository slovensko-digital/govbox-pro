<% flash.each do |type, msg| %>
  <div data-turbo-temporary class="border-t border-b <%= type == "notice" ? "bg-green-100 border-green-500 text-green-700" : "bg-red-100 border-red-500 text-red-700" %> px-4 py-3 m-4 mb-0" role="alert">
    <p class="font-bold"><%= msg %></p>
  </div>
<% end %>
