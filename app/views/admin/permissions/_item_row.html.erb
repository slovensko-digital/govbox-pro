<div id="<%= id %>" class="flex p-4 justify-between items-center gap-4">
  <div class="flex flex-col justify-start items-start flex-grow relative gap-1">
    <%= yield %>
  </div>
  <%= button_to delete_path, method: :delete, title: t('admin.groups.permissions.actions.delete'), data: { turbo_frame: "_top" } do %>
    <%= render Common::DeleteButtonComponent.new %>
  <% end %>
</div>
