<%= turbo_frame_tag(
      "next-link-"+@next_cursor.to_s.tr(' :-',''),
      loading: :lazy,
      src: tag_message_threads_url(tag_id: params[:tag_id], :cursor => @next_cursor, format: :turbo_stream),
      target: "_top",
    ) %>
