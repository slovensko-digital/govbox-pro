<%= turbo_stream.replace MessageDraftHelper.attachments_tag_id(@message) do %>
  <%= render MessageDraftAttachmentsListComponent.new(message: @message) %>
<% end %>

<%= turbo_stream.replace MessageDraftHelper.body_tag_id(@message) do %>
  <%= render MessageDraftBodyComponent.new(message: @message) %>
<% end %>
