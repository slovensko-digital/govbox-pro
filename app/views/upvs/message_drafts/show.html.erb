<%= render MessageThreadComponent.new(
  message_thread: @message_thread,
  thread_tags: @thread_tags,
  thread_messages: @thread_messages,
  thread_last_message_draft_id: @thread_last_message_draft_id
) %>
