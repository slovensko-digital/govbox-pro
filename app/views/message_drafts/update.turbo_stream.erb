<%= turbo_stream.replace dom_id(@message, :body) do %>
  <%= render MessageDraftBodyComponent.new(message: @message) %>
<% end %>

<%= turbo_stream.update dom_id(@message, :submission) do %>
  <% if @message.submittable? %>
    <%= form_with(url: submit_message_draft_path(@message), method: :post, data: { turbo_frame: '_top' }, local: true) do |form| %>
      <%= form.submit "Odoslať", data: { turbo_frame: '_top' }, class: "flex justify-strech items-start self-stretch flex-grow-0 flex-shrink-0 gap-2 p-6 text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-3.5 py-2.5 dark:bg-blue-600 dark:hover:bg-blue-700 focus:outline-none dark:focus:ring-blue-800" %>
    <% end %>
  <% end %>
<% end %>
