<%= render NewMessageDraftComponent.new(
  message: @message,
  templates_list: @templates_list,
  boxes: @boxes,
  recipients_list: @recipients_list,
  selected_box_params: @box_params,
  selected_message_template: @message_template
)%>
