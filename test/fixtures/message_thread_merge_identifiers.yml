# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  message_thread: one
  merge_identifier: <%= SecureRandom.uuid %>

two:
  message_thread: one
  merge_identifier: <%= SecureRandom.uuid %>

three:
  message_thread: two
  merge_identifier: <%= SecureRandom.uuid %>
